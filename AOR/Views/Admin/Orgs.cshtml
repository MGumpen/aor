@model IEnumerable<AOR.Models.OrgModel>

@{
    Layout = "_Layout";
    ViewData["Title"] = "Admin - Dashboard";
}

<link rel="stylesheet" href="~/css/Admin/Orgs.css" />


<section class="container align-content-center">
    
    <div class="site-header d-flex align-items-center justify-content-between mb-3 position-relative">
        <a asp-controller="Admin" asp-action="Index" class="btn btn-primary">Back</a>
        <h1 class="position-absolute top-50 start-50 translate-middle mb-0 text-center">All organizations</h1>
        <a asp-controller="Admin" asp-action="NewOrg" class="btn btn-secondary">Add organization</a>
    </div>

    <table class="table table-striped">
    <thead>
    <tr>
        <th>Org Nr</th>
        <th>Organization</th>
        <th>Edit organization</th>
    </tr>
    </thead>
    <tbody>
        @foreach (var org in Model)
        {
            <tr>
                <td>@org.OrgNr</td>
                <td>@org.OrgName</td>
                <td>
                    <a asp-controller="Admin" asp-action="EditOrg" asp-route-id="@org.OrgNr" class="btn btn-info btn-sm me-1">
                        Edit organization
                    </a>
                </td>
            </tr>
        }
    </tbody>
        
    </table>

</section>